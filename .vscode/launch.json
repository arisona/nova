{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Nova Server",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/server/target/debug/nova-server",
      "args": [],
      "cwd": "${workspaceFolder}/server",
      "preLaunchTask": "server-build"
    }
  ]
}
