[package]
name = "backend-rust"
version = "0.1.0"
edition = "2024"

[dependencies]
actix-web = "4"
actix-files = "0.6"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1"

miniquad = "0.4"
glam = { version = "0.30", features = ["scalar-math"] }
